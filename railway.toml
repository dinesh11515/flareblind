[build]
builder = "DOCKERFILE"
dockerfilePath = "engine/Dockerfile"
watchPatterns = ["engine/**"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 300
